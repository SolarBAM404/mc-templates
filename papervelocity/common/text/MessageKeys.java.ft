package ${MAIN_CLASS.packageName}.text;

public final class MessageKeys {

    public static final String STARTUP_LOADED = "startup.loaded";
    public static final String COMMAND_VERSION = "command.version";

    private MessageKeys() {
    }
}
