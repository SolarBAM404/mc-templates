package ${MAIN_CLASS.packageName};

import java.util.logging.Logger;

public final class Constants {

    public static final String MOD_ID = "${MOD_ID}";
    public static final String MOD_NAME = "${MOD_NAME}";
    public static final String MINECRAFT_VERSION = "${MINECRAFT_VERSION}";
    public static final int BSTATS_PLUGIN_ID = ${BSTATS_PLUGIN_ID};
    public static final Logger LOG = Logger.getLogger(MOD_ID);

    private Constants() {
    }
}
